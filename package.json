{
  "name": "cloudflare-inbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run @cloudflare-inbox/web#build",
    "check": "vp check && node tooling/scripts/check-boundaries.mjs",
    "check:boundaries": "node tooling/scripts/check-boundaries.mjs",
    "check:generated": "node tooling/scripts/check-generated.mjs",
    "dev": "wrangler d1 migrations apply DB --local --config wrangler.jsonc && vp run @cloudflare-inbox/web#dev",
    "typegen": "vp run @cloudflare-inbox/web#cf-typegen",
    "test": "vp test",
    "test:integration": "vp run -r test:integration",
    "test:e2e": "vp run -r test:e2e",
    "test:boundaries": "node --test tooling/scripts/check-boundaries.test.mjs",
    "db:generate": "vp run @cloudflare-inbox/db#db:generate",
    "db:migrate": "wrangler d1 migrations apply DB --remote --config wrangler.jsonc",
    "db:migrate:local": "wrangler d1 migrations apply DB --local --config wrangler.jsonc",
    "deploy": "vp run @cloudflare-inbox/web#build && wrangler d1 migrations apply DB --remote --config wrangler.jsonc && wrangler deploy --config apps/web/dist/server/wrangler.json"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.18.0",
      "onFail": "error"
    },
    "runtime": {
      "name": "node",
      "version": "24.18.1",
      "onFail": "error"
    }
  },
  "engines": {
    "node": "24.18.1",
    "pnpm": "11.18.0"
  },
  "packageManager": "pnpm@11.18.0",
  "cloudflare": {
    "bindings": {
      "AUTH_TOKEN_PEPPER": {
        "description": "Generate a new random value of at least 32 bytes. It protects sign-in and API tokens."
      },
      "SETUP_TOKEN": {
        "description": "Generate a different random value of at least 32 bytes. You will enter it once in the setup wizard."
      }
    }
  }
}
